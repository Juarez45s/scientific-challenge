<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <title>Comienzo</title>
    <link rel="stylesheet" href="/public/styles.css"> <!-- Aquí se puede vincular también un archivo CSS externo -->
    <style>
        body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 150vh;
            margin: 0;
            font-family: Arial, sans-serif;
            background: linear-gradient(to bottom, #000080, #008080);
            text-align: center;
            color: #000;
        }

        .profile-link {
            position: absolute;
            top: 20px;
            right: 10px;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #fff;
            border: 2px solid #fff;
        }

        .profile-link svg {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
        }

        h1 {
            margin-bottom: 150px;
            font-size: 2em;
            color: #5af683;
        }

        .button-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
            margin-bottom: 99px;
        }

        .main-button {
            display: block;
            width: 250%;
            max-width: 300px;
            padding: 15px 0;
            font-size: 1.1em;
            border: none;
            border-radius: 8px;
            background-color: #fff;
            color: #00CC66;
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s;
        }

        .buttoon-comienzo  {
            display: block;
            width: 250%;
            max-width: 300px;
            padding: 15px 0;
            font-size: 1.1em;
            border: none;
            border-radius: 8px;
            background-color: #fff;
            color: #020202;
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s;
        }

        .main-button:hover {
            background-color: #00CC66;
            color: #fff;
        }

        .logout-button {
            margin-top: 1px;
            padding: 10px 20px;
            font-size: 1em;
            border: none;
            border-radius: 5px;
            background-color: #ff4d4d;
            color: #fff;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .logout-button:hover {
            background-color: #cc0000;
        }

        /* Media queries para pantallas pequeñas como teléfonos y tabletas */
        @media (max-width: 768px) {
            .container {
                max-width: 100%;
                padding: 15px;
            }

            button {
                font-size: 14px;
                padding: 8px 12px;
                margin-top: 5px;
            }

            input[type="text"] {
                font-size: 14px;
                padding: 6px;
            }

            h2 {
                font-size: 20px;
            }

            .share-code {
                font-size: 16px;
            }
        }

        @media (max-width: 480px) {
            .container {
                padding: 10px;
            }

            button {
                font-size: 12px;
                padding: 6px 10px;
                margin-top: 5px;
            }

            input[type="text"] {
                font-size: 12px;
                padding: 5px;
            }

            h2 {
                font-size: 18px;
            }

            .share-code {
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <a href="/profile" class="profile-link">
        <!-- El SVG aquí se actualizará dinámicamente con JavaScript -->
    </a>
    <br>
    <br>
    <br>
    <br>
    <br>
    <h1>Aprende ciencias jugando</h1>

    <div class="button-container">
        <button class="main-button" onclick="location.href='/caida-libre'">Caída Libre</button>
        <button class="main-button" onclick="location.href='/movimiento-parabolico'">Movimiento Parabólico</button>
        <button class="main-button" onclick="location.href='/las-3-leyes-newthon'">Las 3 Leyes de Newthon</button>
        <button class="main-button" onclick="location.href='/EL-principio-de-pascal'">El Principio de Pascal</button>
        <button class="main-button" onclick="location.href='/Tenciones'">Tenciones</button>
        <button class="main-button" onclick="location.href='/Vectores'">Vectores</button>
        <button class="main-button" onclick="location.href='/cinematica'">Cinematica</button>
        <button class="main-button" onclick="location.href='/electromagnetismo'">Electromagnetismo</button>
        <button class="buttoon-comienzo" onclick="location.href='/menu'">Comenzar</button>
    </div>

    

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const profileLink = document.querySelector('.profile-link');
            const savedSvg = localStorage.getItem('avatarSvg');

            if (savedSvg) {
                profileLink.innerHTML = savedSvg;
            } else {
                // Si no hay un avatar guardado, mostrar un SVG por defecto
                profileLink.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" fill="#e0e0e0"/><text x="12" y="15" font-size="10" text-anchor="middle" fill="#000">SVG</text></svg>';
            }
        });
    </script>
</body>
</html>
